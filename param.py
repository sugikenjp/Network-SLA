# param.py - SLA監視スクリプト パラメータ定義（1チーム専用 Docker版）
# =====================================================================
# 概要:
#   このコンテナが担当する1チーム分のパラメータを定義するファイル。
#   WEBコンソール（app.py）から保存すると自動上書きされる。
#   手動で直接編集する場合も次回の cron 実行時に反映される。
#
# マルチチーム版からの変更点:
#   - IPアドレスはリストから単一の文字列値に変更
#     （例: sla01_rt_host = "1.1.1.1"）
#   - task01〜09 / fusei はリストからスカラー値に変更
#     （例: task01 = 0）
#   - num_of_team は削除（1チーム専用のため不要）
#   - team_name を新たに追加
#
# チームを追加するには:
#   このリポジトリごとコピーして team_name と各 IP を変更し、
#   docker-compose.yml の TEAM_NAME を合わせて起動する。


# ─────────────────────────── チーム識別情報 ───────────────────────────

# このコンテナが担当するチーム名。
# docker-compose.yml の TEAM_NAME 環境変数と必ず一致させること。
# InfluxDB の Measurement 名（テーブル名）としても使用される。
team_name = "team01"


# ─────────────────────────── SLA01〜05: ping ターゲット IP ───────────────────────────

sla01_rt_host = "1.1.1.1"    # SLA01 の ping 対象 IP
sla02_rt_host = "1.1.2.2"    # SLA02 の ping 対象 IP
sla03_rt_host = "1.1.3.3"    # SLA03 の ping 対象 IP
sla04_rt_host = "1.1.4.4"    # SLA04 の ping 対象 IP
sla05_rt_host = "1.1.5.5"    # SLA05 の ping 対象 IP


# ─────────────────────────── SLA06: HTTP チェック ターゲット IP ───────────────────────────
#
# sla.py が "http://<この IP>" に HTTP GET を送り、
# レスポンスに "menu" が含まれるか判定する。

sla06_web_vip = "192.168.12.1"    # SLA06 の HTTP 確認対象 IP


# ─────────────────────────── SLA スコア定義 ───────────────────────────

sla01_score = 100    # SLA01 成功時の加算ポイント
sla02_score = 100    # SLA02 成功時の加算ポイント
sla03_score = 100    # SLA03 成功時の加算ポイント
sla04_score = 100    # SLA04 成功時の加算ポイント
sla05_score = 100    # SLA05 成功時の加算ポイント
sla06_score = 100    # SLA06 成功時の加算ポイント


# ─────────────────────────── InfluxDB 接続情報 ───────────────────────────
#
# Docker コンテナから外部の InfluxDB サーバーへの接続情報。
# db_host には InfluxDB サーバーの IP アドレスまたはホスト名を設定する。
# ※ "localhost" はコンテナ自身を指すため、外部サーバーには使用しないこと。

db_host     = "192.168.1.100"   # InfluxDB サーバーの IP アドレス（要変更）
db_port     = "8086"            # InfluxDB のポート番号（デフォルト）
db_username = "user"            # InfluxDB 認証ユーザー名
db_password = "user"            # InfluxDB 認証パスワード
db_name     = "bc_db"           # 使用するデータベース名（全チーム共通）


# ─────────────────────────── HTTP タイムアウト ───────────────────────────

# SLA06 の HTTP チェックで requests.get() に渡すタイムアウト秒数。
http_timeout = 7.0


# ─────────────────────────── タスクポイント ───────────────────────────
#
# 大会のチャレンジタスク完了時に加算されるポイント。
# 初期値はすべて 0（未完了）。
# 大会運営者がタスク完了を確認したら該当する値を加算ポイント数に変更する。
#
# 注意:
#   sla.py は毎回のチェックでこの値を「今回分のポイント」として累積加算する。
#   設定した値が毎回加算され続けるため、完了後は適切なタイミングで 0 に戻すこと。

task01 = 0
task02 = 0
task03 = 0
task04 = 0
task05 = 0
task06 = 0
task07 = 0
task08 = 0
task09 = 0


# ─────────────────────────── 不正行為減点 ───────────────────────────
#
# 不正行為による減点ポイント（通常は 0）。
# sum_point2（総合累積スコア）から毎回差し引かれる。

fusei = 0
